name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Install SQLite
        run: sudo apt-get update && sudo apt-get install -y libsqlite3-dev python3
      - name: Test
        run: cargo test --workspace -- --nocapture
