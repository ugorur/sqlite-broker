[package]
name = "sqlite-broker-proto"
version.workspace = true
edition = "2021"
description = "Wire format for sqlite-broker"
license.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
